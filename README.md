## Uma maneira automatizada de obter seus certificados do Even3
Para usar, apenas instale as bibliotecas necessárias listadas em ```requirements.txt```
```
pip install -r requirements.txt
```

adicione seu cookie de sessão na variável ```COOKIE``` em ```utils.py``` e execute

```
python3 main.py
``` 
